# Code Studio
1. ChatBot using RAG pipeline
   - Implements a Retrieval-Augmented Generation (RAG) pipeline.
   - I have used `nltk` for tokenizing and other NLP modalities.
   - `Beautiful Soup` is used for collecting text data from websites.
   - I have used $deepseek-rlt2-chimera$ model.
   - `pdf` of the jupyter notebook is attached.
   - For local implementation of the code enter you api key in the `api_key` variable.

